# Core AI Trading Agent Requirements
# Voice AI agent system for natural language trading

# Core Dependencies (already in main requirements)
# pandas>=1.5.0
# numpy>=1.20.0
# sqlite3 (built-in)
# requests>=2.28.0

# AI/LLM Integration
openai>=1.0.0                    # OpenAI GPT-4 API for NLU
anthropic>=0.7.0                 # Alternative LLM provider (optional)

# Voice Interface (Optional - for voice commands)
SpeechRecognition>=3.10.0        # Speech-to-text
pyttsx3>=2.90                    # Text-to-speech
pyaudio>=0.2.11                  # Audio input/output
portaudio19-dev                  # Audio driver (Linux/Mac)

# JSON Schema Validation
jsonschema>=4.17.0               # Intent validation

# Additional Utilities
python-dateutil>=2.8.0          # Date parsing
pathlib2>=2.3.7                 # Path handling (Python < 3.4)
uuid                             # Request ID generation (built-in)

# Development/Testing (Optional)
pytest>=7.0.0                   # Unit testing
pytest-asyncio>=0.21.0          # Async testing
mock>=4.0.0                     # Mocking for tests

# Voice Dependencies Installation Notes:
# 
# Windows:
#   pip install SpeechRecognition pyttsx3 pyaudio
#
# macOS:
#   brew install portaudio
#   pip install SpeechRecognition pyttsx3 pyaudio
#
# Linux (Ubuntu/Debian):
#   sudo apt-get install portaudio19-dev python3-pyaudio
#   pip install SpeechRecognition pyttsx3 pyaudio
#
# If voice features are not needed, the agent will work without these packages